export { default as LoginScreen } from "@/app/auth/LoginScreen";
export { default as SignUpScreen } from "@/app/auth/signUpScreen";

export const unstable_settings = {
  initialRouteName: true, // Prevent these from being part of the TabNavigator
};
